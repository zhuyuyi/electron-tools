import HomePage from '@/pages/HomePage';
import Person from '@/pages/Person';

const routes = [
    {
        path: '/',
        component: HomePage,
    },
    {
        path: '/person',
        component: Person,
    },
];

export default routes;
